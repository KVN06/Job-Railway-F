{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "export:html": "node scripts/export-blade-to-html.js",
        "export:html:clean": "node scripts/export-blade-to-html.js --clean",
        "views:remove": "node scripts/remove-views.js --confirm",
        "views:remove:force": "node scripts/remove-views.js --confirm --force"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "tailwindcss": "^4.0.0",
        "vite": "^6.2.4"
    }
}
